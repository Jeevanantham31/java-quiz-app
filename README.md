# Online Quiz System (Java + JDBC + MySQL)

Project Overview
------------------
A simple console-based quiz application built using Core Java, JDBC, and MySQL.  
This project follows a layered architecture — separating database logic, business logic and user interface for better structure and scalability.

Key Features
------------------
Take a multiple-choice quiz directly in the console  

Fetch questions dynamically from a MySQL database  

Evaluate and display the user's score  

Modular layered architecture (DAO, Service, UI)  

Uses **PreparedStatement** for secure SQL queries (prevents SQL injection)

